services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: investment
      POSTGRES_PASSWORD: investment
      POSTGRES_DB: investment_os
    ports:
      - '5432:5432'
    volumes:
      - investment-os-postgres:/var/lib/postgresql/data

volumes:
  investment-os-postgres:
